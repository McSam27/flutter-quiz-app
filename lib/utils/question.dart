class Question {
  final String question;
  final bool answer;

  // Create constructor for OOP
  // Question(String question, bool answer) {
  //   this.question = question;
  //   this.answer = answer;
  // }

  // Dart simplifies this can do this instead:
  // Dart constructor
  Question(this.question, this.answer);

}